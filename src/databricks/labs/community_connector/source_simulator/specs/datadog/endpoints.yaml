endpoints:
  - path: /api/v1/hosts
    method: GET
    corpus: hosts
    response:
      pagination_style: none
      wrapper:
        records_key: host_list

  - path: /api/v2/incidents
    method: GET
    corpus: incidents
    response:
      pagination_style: none
      wrapper:
        records_key: data

  - path: /api/v1/monitor
    method: GET
    corpus: monitors
    response:
      pagination_style: none

  - path: /api/v2/security_monitoring/signals
    method: GET
    corpus: signals
    response:
      pagination_style: none
      wrapper:
        records_key: data

  - path: /api/v2/services/definitions
    method: GET
    corpus: services
    response:
      pagination_style: none
      wrapper:
        records_key: data
