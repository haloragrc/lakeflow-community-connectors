# KnowBe4 API Notes (Phase-1)

Primary reporting API endpoints used:
- `GET /v1/users`
- `GET /v1/groups`
- `GET /v1/phishing/campaigns`
- `GET /v1/training/campaigns`
- `GET /v1/training/enrollments`

This implementation is simulator-first with deterministic fake fixtures and no live credentials.
