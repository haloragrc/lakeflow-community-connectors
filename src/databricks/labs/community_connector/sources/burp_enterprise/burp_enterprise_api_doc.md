# Burp Enterprise API Notes (Phase-1)

Primary API pattern used in this spike:
- GraphQL API (`POST /graphql`) for sites, scans, scan results, findings, and scan configurations.

This implementation is simulator-first with deterministic fake fixtures and no live credentials.
