# PagerDuty API Notes (Phase-1)

Primary API endpoints used:
- `GET /users`
- `GET /services`
- `GET /incidents`
- `GET /escalation_policies`
- `GET /oncalls`

This implementation is simulator-first with deterministic fake fixtures and no live credentials.
